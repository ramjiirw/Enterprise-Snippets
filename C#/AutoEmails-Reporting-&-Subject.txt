//Place in strings section
string TaskTitleText  = "Questionnaire Due AP(E)"; 

//Subject
string SubjectText = "Questionnaire Due " + CaseDetails;

//place in strings section - copy whichever is approriate for your DD
//Main case table
string CaseDetails = Refval + " " + SiteAddress;
string Refval = data.REFVAL; 
string SiteAddress = data.ADDRESS.Replace("\r"," "); 

//1 table down
string CaseDetails = Refval + " " + SiteAddress;
string Refval = data.LUPKEYVAL.REFVAL;
string SiteAddress = data.LUPKEYVAL.ADDRESS.Replace("\r"," ");

//2 table down
string CaseDetails = Refval + " " + SiteAddress;
string Refval = data.LUPKEYVAL.LUPKEYVAL.REFVAL;
string SiteAddress = data.LUPKEYVAL.LUPKEYVAL.ADDRESS.Replace("\r"," ");


//Place in reporting section
task.TaskTitle = TaskTitleText;
task.TaskHint = Refval;
task.TaskHelp = SiteAddress;
